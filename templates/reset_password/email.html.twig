{# Legacy template kept for reference; PasswordResetMailerService uses email/password_reset.html.twig #}
{% include 'email/password_reset.html.twig' with {
    user: user,
    resetToken: resetToken,
    webResetUrl: webResetUrl|default(url('app_reset_password', {token: resetToken.token})),
    appResetUrl: appResetUrl|default('mifania://reset-password?token=' ~ resetToken.token|url_encode),
} only %}
