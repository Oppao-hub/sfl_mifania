{% extends 'dashboard/dashboard.html.twig' %}

{% block title %}Add Sub Category{% endblock %}

{% block main_content %}
<div class="mx-1 min-h-screen bg-white p-8 rounded-xl shadow-md">
    <h1 class="text-2xl font-bold text-mocha mb-6">Add New Sub Category</h1>

    {{ include('dashboard/sub_category/_form.html.twig', {
        form: form,
        button_label: 'Add Sub Category',
        button_class: 'px-3 py-2 bg-success text-white text-sm font-semibold rounded-xl hover:bg-success-hover'
    }) }}
</div>
{% endblock %}
