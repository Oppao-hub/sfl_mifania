{% extends 'dashboard/dashboard.html.twig' %}

{% block title %}Edit Sub Category: {{ subCategory.name }} {% endblock %}

{% block main_content %}
<div class="mx-1 min-h-screen bg-white p-8 rounded-xl shadow-md">
    <h1 class="text-2xl font-bold text-brand mb-6">Edit Sub Category: {{ subCategory.name }}</h1>

        {{ include('dashboard/sub_category/_form.html.twig', {
            form: form,
            button_label: 'Update Sub Category',
            button_class: 'cursor-pointer px-4 py-2 bg-success text-sm font-semibold text-white rounded-xl hover:bg-success-hover'
        }) }}

</div>
{% endblock %}
