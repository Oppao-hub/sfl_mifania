{% extends 'admin/dashboard.html.twig' %} {% block title %}Edit Order{% endblock
%} {% block main_content %}
<div class="form-wrapper">
    <div class="form-container">
        <div class="form-header">
            <h1>Edit Order</h1>
        </div>
        {{
            include("admin/order/_form.html.twig", {
                button_label: "Update"
            })
        }}
        <div class="form-action">
            {{ include("admin/order/_delete_form.html.twig") }}
            <a href="{{ path('app_admin_order_index') }}">Back</a>
        </div>
    </div>
</div>
{% endblock %}
