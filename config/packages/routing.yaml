parameters:
    app.default_uri: 'https://sfl-mifania.up.railway.app'

framework:
    router:
        # Required for absolute URLs in emails (async messenger / CLI).
        default_uri: '%env(default:app.default_uri:DEFAULT_URI)%'

when@prod:
    framework:
        router:
            strict_requirements: null
